# jvmargs stay at the default, so Gradle does not fork a second JVM here
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
